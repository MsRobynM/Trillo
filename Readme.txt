Package.json script notes:

Need to install for prefixer:

npm install autoprefixer --save-dev

 - and -

npm install postcss-cli --save-dev


---------

build:css requires

npm install npm-run-all --save-dev
